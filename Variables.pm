package Variables;
use Exporter 'import';
our @EXPORT_OK  = qw($i $fh $NUM_REQ $TX_SIZE $ADDR_LEN $TYPE_PATTERN $switch $ADDR_PATTERN $CHANNEL_PATTERN $RANK_PATTERN $BANK_PATTERN $ROW_PATTERN $num_hits $locality $BankInterleave $RankInterleave $ChannelInterleave $COLUMN_PATTERN $TARGET_COLUMN $TARGET_ROW $TARGET_BANK $TARGET_RANK $TARGET_CHANNEL $TARGET_ADDRESS $sw=0 $ROW $BANK $RANK $COLUMN $CHANNEL $ColumnMask $RowMask $RankMask $BankMask $ChannelMask $LSB_ROW $LSB_COLUMN $LSB_BANK $LSB_RANK $LSB_CHANNEL $num_row_bits $num_column_bits $num_rank_bits $num_bank_bits $num_channel_bits $num_rows $num_banks $num_ranks $num_channels $num_columns);




